
 const options = {
    lemon:  'yellow',
    lime: 'limegreen',
    tangerine: 'orange',
    grapefruit: 'lightcoral'
  };

const controller = {
  // add a method called 'getColor'
}

// Export the controller object
